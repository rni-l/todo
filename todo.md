# TODO

## v1.6

状态：待完成

1. 统计报表页面的入口没有在侧边导航栏显示
2. 侧边导航栏支持滚动条
3. 任务支持关闭，关闭和完成不一样
4. 日期提醒支持时间范围，日历视图要同步显示范围

## v1.5

状态：已完成

1. 新建任务，支持连续新建，只清空“任务标题”
2. 日历视图支持点击选中某个日期，然后点击“新建任务”的时候，日期自动关联对应的日期
3. 日历视图的文字大小、间距继续缩小；每个任务的左侧高亮块移除，改为边框高亮
4. 设计统计报表页面

## v1.4

状态：已完成

1. 增加 release snapshot 打包流程，生成 `.deploy/releases/<release>` 和 `.deploy/current`
2. PM2 改为固定运行 `.deploy/current`，开发工作区和线上运行目录彻底隔离
3. 默认继续使用现有 `./data` 数据源，日志输出到 `.deploy/shared/logs`
4. 发布命令：
   `npm run release:build`
   `npm run pm2:start`
   `npm run pm2:restart`

## v1.3

状态：已完成

1. 没找到新建项目和编辑项目多功能？
2. 项目支持归档，归档后，在“项目”列表隐藏，并且左侧新建一个”归档项目“的列表，专门显示已归档的项目，默认收缩；已归档的项目支持恢复
3. 项目列表没有显示所有项目数据？
4. CheckList 的“粘贴多行内容”，点击添加后，不会生成多个子项

## v1.2

状态：已完成

1. 日历视图修改
   1.1 默认“月”范围
   1.2 每日显示多少条任务想，支持可配置化
   1.3 需要根据“优先级”显示不同颜色（这个优先级需要统一化，每个模块显示都是同一个逻辑）
   1.4 需要显示星期几
2. 新建任务弹窗
   2.1 弹窗高度增加多 100px
3. Checklist 相当于子任务，需要支持像“任务”那样，有日期、优先级这两个配置
4. 优化前端缓存逻辑，每次功能迭代要更新版本号，然后重启服务再刷新浏览器，就能访问最新版本；目前需要强制禁用缓存，才会访问到最新版本

## v1.1

状态：已完成

1. 任务清单，需要根据任务的日期时间进行标识，分四种状态：已过期（红色）、今天的（绿色）、未来的（蓝色）、没有标记时间的（灰色）
2. 最近7天的视图逻辑：
   2.1 已过期的都显示
   2.2 从今天开始算，到未来7天的任务清单
3. markdown 描述，把“渲染”那部分隐藏，只需输入框即可
